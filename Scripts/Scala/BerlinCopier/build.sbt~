name := "BerlinCopier"
organization := "UCR-DBLab"
version := "0.2"

scalaVersion in ThisBuild := "2.11.8"

val SparkVersion = "2.1.0"
val dependencyScope = "compile"

libraryDependencies ++= Seq(
  "org.apache.spark" %% "spark-core" % "2.1.0" % dependencyScope exclude("org.apache.hadoop", "*"),
  "org.apache.spark" %% "spark-sql" % "2.1.0" % dependencyScope exclude("org.apache.hadoop", "*"),
  "org.slf4j" % "slf4j-jdk14" % "1.7.25"
)

mainClass in (Compile, run) := Some("BerlinCopier")
mainClass in (Compile, packageBin) := Some("BerlinCopier")
